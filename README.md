# Carmera

Simple set of three apps:
 
 -> App written in Angular acting as a streaming server dedicated to run on RaspberryPi,
 
 -> Second Angular app for displaying streams on client side,
 
 -> Signaling server based on .net core.
 
